# BLINDTEST PROJECT

INITIALISATION

- npm init

- npm install electron - -save-dev 

Dans ‘package.json’ :

```
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
```

- npm run start